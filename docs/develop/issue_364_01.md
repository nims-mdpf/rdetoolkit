# Issue #364 - Task 01: Create package structure and helpers module

## Overview
Create the new `invoicefile/` package structure and extract module-level utility functions and constants into `_helpers.py`. This establishes the foundation for all subsequent extraction tasks.

## Related Information
- **Input Source**: `local/develop/issue_364.md`
- **Overview Document**: `docs/develop/issue_364_overview.md`
- **Dependencies**: None (foundational task)

## Target Files
- [ ] `src/rdetoolkit/invoicefile/` (new directory)
- [ ] `src/rdetoolkit/invoicefile/_helpers.py` (new file)
- [ ] `tests/invoicefile/` (new directory)
- [ ] `tests/invoicefile/test_helpers.py` (new file - optional, for verification)

## Implementation Steps

### Step 1: Create package directory structure
```bash
mkdir -p src/rdetoolkit/invoicefile
mkdir -p tests/invoicefile
```

### Step 2: Create `_helpers.py` with module-level utilities

Extract lines 31-73 from `invoicefile.py`:

```python
# src/rdetoolkit/invoicefile/_helpers.py
"""Helper utilities for invoicefile package.

This module provides lazy import helpers and shared constants used across
the invoicefile package modules.
"""
from __future__ import annotations

import re
from collections.abc import Callable
from pathlib import Path
from typing import Any

# Module constants
STATIC_DIR = Path(__file__).parent.parent / "static"
EX_GENERALTERM = STATIC_DIR / "ex_generalterm.csv"
EX_SPECIFICTERM = STATIC_DIR / "ex_specificterm.csv"
MAGIC_VARIABLE_PATTERN = re.compile(r"\$\{([^{}]+)\}")


def _ensure_pandas() -> Any:
    """Lazy import of pandas library."""
    import pandas as pd
    return pd


def _ensure_openpyxl_styles() -> tuple[Any, Any, Any]:
    """Lazy import of openpyxl style classes."""
    from openpyxl.styles import Border, Font, Side
    return Border, Font, Side


def _ensure_openpyxl_utils() -> Any:
    """Lazy import of openpyxl utilities."""
    from openpyxl.utils import get_column_letter
    return get_column_letter


def _ensure_chardet() -> Any:
    """Lazy import of chardet library."""
    import chardet
    return chardet


def _ensure_validation_error() -> type[Exception]:
    """Lazy import of pydantic ValidationError."""
    from pydantic import ValidationError
    return ValidationError


def _ensure_logger() -> Callable[..., Any]:
    """Lazy import of rdetoolkit logger."""
    from rdetoolkit.rdelogger import get_logger
    return get_logger


# Module-level logger instance
logger = _ensure_logger()(__name__)
```

**Note**: The `STATIC_DIR` path changes from `Path(__file__).parent / "static"` to `Path(__file__).parent.parent / "static"` because the new module is one level deeper in the directory hierarchy.

### Step 3: Create minimal `__init__.py` (placeholder)

```python
# src/rdetoolkit/invoicefile/__init__.py
"""Invoice file handling package.

This package provides utilities for working with RDE invoice files
in JSON and Excel formats.
"""
from __future__ import annotations

# Re-exports will be added in subsequent tasks
__all__ = []
```

### Step 4: Verify imports work

Create a simple verification test:

```python
# tests/invoicefile/test_helpers.py
"""Tests for invoicefile._helpers module."""
from __future__ import annotations

import re
from pathlib import Path


def test_helpers_imports():
    """Verify all helper utilities can be imported."""
    from rdetoolkit.invoicefile._helpers import (
        EX_GENERALTERM,
        EX_SPECIFICTERM,
        MAGIC_VARIABLE_PATTERN,
        STATIC_DIR,
        _ensure_chardet,
        _ensure_logger,
        _ensure_openpyxl_styles,
        _ensure_openpyxl_utils,
        _ensure_pandas,
        _ensure_validation_error,
        logger,
    )

    # Verify constants
    assert isinstance(STATIC_DIR, Path)
    assert isinstance(EX_GENERALTERM, Path)
    assert isinstance(EX_SPECIFICTERM, Path)
    assert isinstance(MAGIC_VARIABLE_PATTERN, re.Pattern)

    # Verify lazy loaders work
    assert _ensure_pandas() is not None
    assert _ensure_logger() is not None
    assert logger is not None


def test_static_dir_path_correct():
    """Verify STATIC_DIR points to correct location."""
    from rdetoolkit.invoicefile._helpers import STATIC_DIR

    # Should point to src/rdetoolkit/static
    assert STATIC_DIR.name == "static"
    assert STATIC_DIR.exists()
```

### Step 5: Run tests

```bash
# Run new tests to verify extraction
tox -e py312-module -- tests/invoicefile/test_helpers.py -v

# Verify no regressions in existing tests
tox -e py312-module -- tests/test_invoicefile.py -v
tox -e py312-mypy
tox -e py312-ruff
```

## Completion Criteria
- [ ] Package directory `src/rdetoolkit/invoicefile/` created
- [ ] `_helpers.py` contains all extracted utilities with correct types
- [ ] `STATIC_DIR` path adjustment verified (parent.parent)
- [ ] All lazy import functions work correctly
- [ ] New tests pass (`tests/invoicefile/test_helpers.py`)
- [ ] Existing tests still pass (no regressions)
- [ ] No new type errors (`tox -e py312-mypy`)
- [ ] Code follows project style (`tox -e py312-ruff`)
- [ ] All comments and docstrings in English

## Notes
- This task is foundational - all subsequent tasks depend on `_helpers.py`
- The `STATIC_DIR` path must be adjusted because the new module is nested one level deeper
- Do not modify `src/rdetoolkit/invoicefile.py` yet - that happens in Task 11
- The logger instance name will be `rdetoolkit.invoicefile._helpers` instead of `rdetoolkit.invoicefile`, which is acceptable
- Consider this a zero-risk change since existing code isn't modified yet
