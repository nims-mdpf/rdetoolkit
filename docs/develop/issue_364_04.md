# Issue #364 - Task 04: Extract template generation classes

## Overview
Extract the `TemplateGenerator` Protocol and `ExcelInvoiceTemplateGenerator` class into `_template.py`. These classes handle Excel invoice template generation from schema definitions.

## Related Information
- **Input Source**: `local/develop/issue_364.md`
- **Overview Document**: `docs/develop/issue_364_overview.md`
- **Dependencies**: Task 01 (requires `_helpers.py`), Task 02 (uses constants from sheet processing)

## Target Files
- [ ] `src/rdetoolkit/invoicefile/_template.py` (new file)
- [ ] `tests/invoicefile/test_template.py` (new file)

## Implementation Steps

### Step 1: Create `_template.py`

Extract lines 409-641 from `invoicefile.py`:

```python
# src/rdetoolkit/invoicefile/_template.py
"""Excel invoice template generation.

This module provides classes for generating Excel invoice templates
from RDE invoice schema definitions.
"""
from __future__ import annotations

from typing import TYPE_CHECKING, Any, Protocol

from rdetoolkit.invoicefile._helpers import (
    EX_GENERALTERM,
    EX_SPECIFICTERM,
    _ensure_openpyxl_styles,
    _ensure_openpyxl_utils,
    _ensure_pandas,
    logger,
)

if TYPE_CHECKING:
    import pandas as pd

    from rdetoolkit.models.invoice import FixedHeaders, TemplateConfig


class TemplateGenerator(Protocol):
    """Protocol for template generators.

    Defines the interface for generating invoice templates from configurations.
    """

    def generate(self, config: TemplateConfig) -> pd.DataFrame:
        """Generates a template based on the provided configuration.

        Args:
            config (TemplateConfig): The configuration object.

        Returns:
            pd.DataFrame: A DataFrame representing the generated template.
        """
        ...


class ExcelInvoiceTemplateGenerator:
    """Generator for Excel invoice templates.

    This class generates Excel invoice templates based on RDE invoice schema
    definitions, including support for general and specific terms.

    Attributes:
        fixed_headers (FixedHeaders): Fixed header configuration for the template.

    Args:
        fixed_headers (FixedHeaders): The fixed headers configuration.

    Example:
        from rdetoolkit.models.invoice import FixedHeaders, TemplateConfig

        headers = FixedHeaders()
        generator = ExcelInvoiceTemplateGenerator(headers)
        config = TemplateConfig(
            schema_path="invoice.schema.json",
            general_term_path="generalterm.csv",
            specific_term_path="specificterm.csv",
            inputfile_mode="file"
        )
        template_df, general_df, specific_df, version_df = generator.generate(config)
    """

    def __init__(self, fixed_headers: FixedHeaders):
        self.fixed_headers = fixed_headers

    def generate(
        self, config: TemplateConfig
    ) -> tuple[pd.DataFrame, pd.DataFrame, pd.DataFrame, pd.DataFrame]:
        """Generate Excel invoice template from configuration.

        Args:
            config: Template configuration containing schema and term paths.

        Returns:
            Tuple of (template_df, general_df, specific_df, version_df).
        """
        # Implementation from lines 437-600 in original file
        # This includes all the complex logic for:
        # - Schema parsing
        # - Header generation
        # - General/specific term loading
        # - Sample value generation
        # - DataFrame construction

        pd = _ensure_pandas()

        # ... (full implementation from original)

        logger.info("Template generation complete")
        return template_df, general_df, specific_df, version_df

    def save(
        self,
        dataframes: dict[str, pd.DataFrame],
        output_path: str,
    ) -> None:
        """Save generated templates to an Excel file with styling.

        Args:
            dataframes: Dictionary mapping sheet names to DataFrames.
            output_path: Path where Excel file will be saved.

        Example:
            dataframes = {
                "invoice_form": template_df,
                "generalTerm": general_df,
                "specificTerm": specific_df,
                "_version": version_df,
            }
            generator.save(dataframes, "template.xlsx")
        """
        # Implementation from lines 600-641
        # Includes openpyxl styling logic

        import openpyxl

        pd = _ensure_pandas()
        Border, Font, Side = _ensure_openpyxl_styles()
        get_column_letter = _ensure_openpyxl_utils()

        # Write DataFrames to Excel
        with pd.ExcelWriter(output_path, engine="openpyxl") as writer:
            for sheet_name, df in dataframes.items():
                df.to_excel(writer, sheet_name=sheet_name, index=False, header=False)

        # Apply styling
        wb = openpyxl.load_workbook(output_path)
        for sheet_name in dataframes:
            if sheet_name in wb.sheetnames:
                ws = wb[sheet_name]
                self._apply_sheet_styling(ws)

        wb.save(output_path)
        logger.info(f"Template saved to {output_path}")

    def _apply_sheet_styling(self, worksheet: Any) -> None:
        """Apply styling to Excel worksheet.

        Args:
            worksheet: openpyxl worksheet object.
        """
        Border, Font, Side = _ensure_openpyxl_styles()

        # Header row styling
        header_font = Font(bold=True)
        thin_border = Border(
            left=Side(style="thin"),
            right=Side(style="thin"),
            top=Side(style="thin"),
            bottom=Side(style="thin"),
        )

        # Apply to all cells (implementation details from original)
        for row in worksheet.iter_rows():
            for cell in row:
                cell.border = thin_border
                if cell.row <= 2:  # Header rows
                    cell.font = header_font
```

### Step 2: Update imports in `__init__.py`

Add to `src/rdetoolkit/invoicefile/__init__.py`:

```python
from rdetoolkit.invoicefile._template import (
    ExcelInvoiceTemplateGenerator,
    TemplateGenerator,
)

# Update __all__
__all__ = [
    # ... existing exports
    "TemplateGenerator",
    "ExcelInvoiceTemplateGenerator",
]
```

### Step 3: Create verification tests

```python
# tests/invoicefile/test_template.py
"""Tests for template generation classes."""
from __future__ import annotations


def test_template_generator_protocol():
    """Verify TemplateGenerator protocol is defined."""
    from rdetoolkit.invoicefile import TemplateGenerator

    assert TemplateGenerator is not None


def test_excel_template_generator_import():
    """Verify ExcelInvoiceTemplateGenerator can be imported."""
    from rdetoolkit.invoicefile import ExcelInvoiceTemplateGenerator

    assert ExcelInvoiceTemplateGenerator is not None


def test_excel_template_generator_instantiation():
    """Verify ExcelInvoiceTemplateGenerator can be instantiated."""
    from rdetoolkit.invoicefile import ExcelInvoiceTemplateGenerator
    from rdetoolkit.models.invoice import FixedHeaders

    headers = FixedHeaders()
    generator = ExcelInvoiceTemplateGenerator(headers)
    assert generator is not None
    assert generator.fixed_headers == headers
```

### Step 4: Run tests

```bash
tox -e py312-module -- tests/invoicefile/test_template.py -v
tox -e py312-module -- tests/test_invoicefile.py::TestExcelInvoiceTemplateGenerator -v
tox -e py312-mypy
tox -e py312-ruff
```

## Completion Criteria
- [ ] `_template.py` created with both classes
- [ ] `TemplateGenerator` Protocol preserved
- [ ] `ExcelInvoiceTemplateGenerator` with all methods extracted
- [ ] Styling logic (`_apply_sheet_styling`) preserved
- [ ] `__init__.py` re-exports both classes
- [ ] New tests pass
- [ ] Existing template generation tests pass
- [ ] No new type errors (`tox -e py312-mypy`)
- [ ] Code follows project style (`tox -e py312-ruff`)

## Notes
- `TemplateGenerator` is a Protocol (structural typing), not an ABC
- `ExcelInvoiceTemplateGenerator` is used by `ExcelInvoiceFile` (Task 05)
- The `generate()` method returns a 4-tuple of DataFrames
- The `save()` method handles Excel styling with openpyxl
- Constants from `_helpers` (EX_GENERALTERM, EX_SPECIFICTERM) are used
- Complex schema parsing logic must be preserved exactly
